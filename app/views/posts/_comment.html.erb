<div class="comment">
  <h4>Comment by <%= comment.author_name %></h4>
  <p><%= comment.body %></p>
  <p>Posted <%= distance_of_time_in_words(comment.post.created_at, comment.created_at) %> ago</p>
</div>